// TODO: Implement.  We basically need a semi-opague rectangle with a text display that
// can position itself when passed a node from the timePopularityGraph (in draw) and also erase itself;

class ToolTip {
    constructor(layout, container, scale, mediator) {
        this.layout = layout;
        this.container = container;
        this.scale = scale;
        this.mediator = mediator;
    }
}